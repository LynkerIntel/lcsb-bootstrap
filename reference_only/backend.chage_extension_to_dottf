terraform {
  backend "s3" {
    bucket         = var.bucket_name
    key            = "tfstate/some/other/statefile"
    region         = var.region
    dynamodb_table = "tfstate_lock_db"
    encrypt        = true
  }
}
# terraform {
#   backend "s3" {
#     bucket         = var.bucket_name
#     key            = "terraform/state"
#     region         = var.region
#     dynamodb_table = aws_dynamodb_table.tfstate_lock_db.name
#   }
# }